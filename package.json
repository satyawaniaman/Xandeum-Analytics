{
  "name": "xandeum-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm -r run lint",
    "lint:fix": "pnpm -r run lint:fix",
    "format": "pnpm -r run format",
    "format:check": "pnpm -r run format:check",
    "build": "pnpm -r run build",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "engines": {
    "node": ">=20"
  }
}
